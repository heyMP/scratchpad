---
"@heymp/scratchpad": minor
---

Add `--pick-browser` flag to the `run` command that lets you choose from installed Chromium versions. When the expected Chromium is missing, scratchpad now auto-selects the newest available build and displays browser info using @clack/prompts note and log helpers.
